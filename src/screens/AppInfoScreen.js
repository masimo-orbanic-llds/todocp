import React from 'react';

import {Screen, Text} from '../components';

function AppInfoScreen(props) {
  const text = 'AppInfoScreen';

  return (
    <Screen>
      <Text>{text}</Text>
    </Screen>
  );
}

export default AppInfoScreen;
